# Copy to .env and fill in values.
# Never commit .env to version control.

# Phase 2 — live data fetching
TWELVE_DATA_API_KEY=      # https://twelvedata.com — commodities + FX
NEWS_API_KEY=             # https://newsapi.org   — global economic headlines

# Optional: delivery targets (Phase 3)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
BRIEFING_RECIPIENTS=
